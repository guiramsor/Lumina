# Dependencias
node_modules/

# Builds
dist/
dist-desktop/

# Android (app movil)
android/build/
android/app/build/
android/.gradle/
# local.properties guarda la ruta del SDK, propia de cada maquina
android/local.properties
android/.idea/
android/app/release/
*.apk
*.aab
*.keystore
*.jks
# Contrasenas de la clave de firma. Perderlo impide volver a publicar
# actualizaciones: hay que guardarlo aparte, no en el repositorio.
android/keystore.properties

# Biblioteca de prueba (varios GB)
audios/

# Credenciales de sincronizacion (ver .env.example)
.env
.env.local

# Accesos directos y ajustes locales de la máquina
Lumina.lnk
.claude/settings.local.json

*.log
